# ========================
# shipping-sapi - DEV
# ========================

# HTTP Configuration
http.host=0.0.0.0
http.port=8086

# Application Info
app.name=shipping-sapi
app.version=1.0.0
app.log.category=com.example.oms.shipping.sapi

# Shipping Backend Configuration
shipping.backend.baseUri=http://placeholder-shipping-backend.example.com
shipping.backend.path=/api/v1/shipments

# Timeout Configuration
shipping.backend.connectionTimeout=5000
shipping.backend.responseTimeout=10000
