# =============================================================================
# Order Process API - Environment configuration (dev)
# =============================================================================

# HTTP Listener
http.port=8082
http.host=0.0.0.0

# Logging
app.name=order-papi
app.version=1.0.0
app.log.category=com.example.oms.order.papi

# System API: Customer
customer.host=localhost
customer.port=8083
customer.protocol=HTTP
customer.basePath=/api
customer.connectionTimeout=5000
customer.responseTimeout=10000

# System API: Inventory
inventory.host=localhost
inventory.port=8084
inventory.protocol=HTTP
inventory.basePath=/api
inventory.connectionTimeout=5000
inventory.responseTimeout=10000

# System API: Payment
payment.host=localhost
payment.port=8085
payment.protocol=HTTP
payment.basePath=/api
payment.connectionTimeout=5000
payment.responseTimeout=10000

# System API: OMS
oms.host=localhost
oms.port=8087
oms.protocol=HTTP
oms.basePath=/api
oms.connectionTimeout=5000
oms.responseTimeout=10000

# System API: Shipping
shipping.host=localhost
shipping.port=8086
shipping.protocol=HTTP
shipping.basePath=/api
shipping.connectionTimeout=5000
shipping.responseTimeout=10000
